local success, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
end)

if not success or not WindUI then
    warn("⚠️ UI failed to load!")
    return
else
    print("✓ UI loaded successfully!")
end

local Window = WindUI:CreateWindow({
    Title = "PAHAJI HUB",
    Icon = "rbxassetid://120865009933371",
    Author = "Pahaji | Fish It",
    Folder = "PAHAJI_HUB",
    Size = UDim2.fromOffset(270, 300),
    Transparent = true,
    Theme = "Dark",
    SideBarWidth = 170,
    HasOutline = true
})

Window:Tag({
    Title = "Premium Version",
    Color = Color3.fromHex("#9b4dff")
})

WindUI:Notify({
    Title = "Pahajihub Loaded",
    Content = "UI loaded successfully!",
    Duration = 3,
    Icon = "bell",
})

local Tab1 = Window:Tab({
    Title = "Home",
    Icon = "house",
})

local Section = Tab1:Section({ 
    Title = "Community Support",
    TextXAlignment = "Left",
    TextSize = 17,
})

Tab1:Button({
    Title = "Discord",
    Desc = "click to copy link",
    Callback = function()
        if setclipboard then
            setclipboard("LAGI IMPROVE")
        end
    end
})

local Section = Tab1:Section({ 
    Title = "Every time there is a game update or someone reports something, I will fix it as soon as possible.",
    TextXAlignment = "Left",
    TextSize = 17,
})

local Tab2 = Window:Tab({
    Title = "Players",
    Icon = "user",
})

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")

local Input = Tab2:Input({
    Title = "WalkSpeed",
    Desc = "Minimum 16 speed",
    Value = "16",
    InputIcon = "bird",
    Type = "Input",
    Placeholder = "Enter number...",
    Callback = function(input) 
        local speed = tonumber(input)
        if speed and speed >= 16 then
            Humanoid.WalkSpeed = speed
            print("WalkSpeed set to: " .. speed)
        else
            Humanoid.WalkSpeed = 16
            print("⚠️ Invalid input, set to default (16)")
        end
    end
})

local Input = Tab2:Input({
    Title = "Jump Power",
    Desc = "Minimum 50 jump",
    Value = "50",
    InputIcon = "bird",
    Type = "Input",
    Placeholder = "Enter number...",
    Callback = function(input) 
        local value = tonumber(input)
        if value then
            _G.CustomJumpPower = value
            local humanoid = game:GetService("Players").LocalPlayer.Character and game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.UseJumpPower = true
                humanoid.JumpPower = value
            end
            print("🔼 Jump Power diatur ke: " .. value)
        else
            warn("⚠️ Harus angka, bukan teks!")
        end
    end
})

local Button = Tab2:Button({
    Title = "Reset Jump Power",
    Desc = "Return Jump Power to normal (50)",
    Callback = function()
        _G.CustomJumpPower = 50
        local humanoid = game:GetService("Players").LocalPlayer.Character and game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.UseJumpPower = true
            humanoid.JumpPower = 50
        end
        print("🔄 Jump Power di-reset ke 50")
    end
})

local Player = game:GetService("Players").LocalPlayer
Player.CharacterAdded:Connect(function(char)
    local Humanoid = char:WaitForChild("Humanoid")
    Humanoid.UseJumpPower = true
    Humanoid.JumpPower = _G.CustomJumpPower or 50
end)

Tab2:Button({
    Title = "Reset Speed",
    Desc = "Return speed to normal (16)",
    Callback = function()
        Humanoid.WalkSpeed = 16
        print("WalkSpeed reset ke default (16)")
    end
})

local UserInputService = game:GetService("UserInputService")
local Player = game.Players.LocalPlayer

local Toggle = Tab2:Toggle({
    Title = "Infinite Jump",
    Desc = "activate to use infinite jump",
    Icon = "bird",
    Type = "Checkbox",
    Default = false,
    Callback = function(state) 
        _G.InfiniteJump = state
        if state then
            print("✅ Infinite Jump Aktif")
        else
            print("❌ Infinite Jump Nonaktif")
        end
    end
})

UserInputService.JumpRequest:Connect(function()
    if _G.InfiniteJump then
        local character = Player.Character or Player.CharacterAdded:Wait()
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

local Tab3 = Window:Tab({
    Title = "Main",
    Icon = "landmark",
})

-- === MODULE AUTO SELL ===
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local function findSellRemote()
    local RS = game:GetService("ReplicatedStorage")
    
    -- Path utama (paling umum di script Fish It 2025-2026)
    local mainPath = RS:FindFirstChild("Packages")
        and RS.Packages:FindFirstChild("_Index")
        and RS.Packages._Index:FindFirstChild("sleitnick_net@0.2.0")
        and RS.Packages._Index["sleitnick_net@0.2.0"]:FindFirstChild("net")
    
    if mainPath then
        -- Coba RF/SellAllItems atau net.RF.SellAllItems
        local rf = mainPath:FindFirstChild("RF")
        if rf then
            local sell = rf:FindFirstChild("SellAllItems")
            if sell then 
                print("✓ SellAllItems ditemukan di path utama: " .. sell:GetFullName())
                return sell 
            end
        end
        
        -- Coba langsung net["RF/SellAllItems"]
        local sellDirect = mainPath:FindFirstChild("RF/SellAllItems")
        if sellDirect then 
            print("✓ SellAllItems ditemukan direct: " .. sellDirect:GetFullName())
            return sellDirect 
        end
    end
    
    -- Fallback 1: Cari semua SellAllItems di ReplicatedStorage
    for _, obj in ipairs(RS:GetDescendants()) do
        if obj.Name == "SellAllItems" and (obj:IsA("RemoteFunction") or obj:IsA("RemoteEvent")) then
            print("✓ Fallback: SellAllItems ditemukan di " .. obj:GetFullName())
            return obj
        end
    end
    
    -- Fallback 2: Cari partial "sellall" atau "sell" (debug candidates)
    print("⚠️ SellAllItems tidak ditemukan di path standar. Mencari kandidat...")
    for _, obj in ipairs(RS:GetDescendants()) do
        local nameLower = obj.Name:lower()
        if nameLower:find("sellall") or nameLower:find("sell") or nameLower:find("itemsell") then
            if obj:IsA("RemoteFunction") or obj:IsA("RemoteEvent") then
                print("   Kandidat possible: " .. obj:GetFullName() .. " (" .. obj.ClassName .. ")")
            end
        end
    end
    
    return nil
end

local SellRemote = findSellRemote()

local function parseNumber(text)
    if not text or text == "" then return 0 end
    local cleaned = tostring(text):gsub("%D", "")
    return tonumber(cleaned) or 0
end

local function getBagCount()
    local gui = player:FindFirstChild("PlayerGui")
    if not gui then return 0, 0 end

    local inv = gui:FindFirstChild("Inventory")
    if not inv then return 0, 0 end

    local label = inv:FindFirstChild("Main")
        and inv.Main:FindFirstChild("Top")
        and inv.Main.Top:FindFirstChild("Options")
        and inv.Main.Top.Options:FindFirstChild("Fish")
        and inv.Main.Top.Options.Fish:FindFirstChild("Label")
        and inv.Main.Top.Options.Fish.Label:FindFirstChild("BagSize")

    if not label or not label:IsA("TextLabel") then return 0, 0 end

    local curText, maxText = label.Text:match("(.+)%/(.+)")
    if not curText or not maxText then return 0, 0 end

    return parseNumber(curText), parseNumber(maxText)
end

local AutoSellSystem = {
    Remote = SellRemote,
    _totalSells = 0,
    _lastSellTime = 0,
    
    Timer = {
        Enabled = false,
        Interval = 5,
        Thread = nil,
        _sellCount = 0
    },
    
    Count = {
        Enabled = false,
        Target = 235,
        CheckDelay = 1.5,
        _lastSell = 0,
        _thread = nil
    }
}

local function executeSell()
    if not SellRemote then return false end
    
    local success = pcall(function()
        if SellRemote:IsA("RemoteFunction") then
            SellRemote:InvokeServer()
        else  -- kalau RemoteEvent
            SellRemote:FireServer()
        end
    end)
    
    if success then
        AutoSellSystem._totalSells = AutoSellSystem._totalSells + 1
        AutoSellSystem._lastSellTime = tick()
        print("✓ SellAllItems berhasil dipanggil! Total: " .. AutoSellSystem._totalSells)
        return true
    else
        warn("❌ Gagal Invoke/Fire SellAllItems: " .. tostring(success))
        return false
    end
end

function AutoSellSystem.SellOnce()
    if not SellRemote then return false end
    if tick() - AutoSellSystem._lastSellTime < 0.5 then return false end
    return executeSell()
end

function AutoSellSystem.Timer.Start(interval)
    if AutoSellSystem.Timer.Enabled then return false end
    if not SellRemote then return false end
    
    if interval and tonumber(interval) and tonumber(interval) >= 2 then
        AutoSellSystem.Timer.Interval = tonumber(interval)
    end
    
    AutoSellSystem.Timer.Enabled = true
    AutoSellSystem.Timer._sellCount = 0
    
    AutoSellSystem.Timer.Thread = task.spawn(function()
        while AutoSellSystem.Timer.Enabled do
            task.wait(AutoSellSystem.Timer.Interval)
            if executeSell() then
                AutoSellSystem.Timer._sellCount += 1
            end
        end
    end)
    
    return true
end

function AutoSellSystem.Timer.Stop()
    AutoSellSystem.Timer.Enabled = false
    if AutoSellSystem.Timer.Thread then
        task.cancel(AutoSellSystem.Timer.Thread)
        AutoSellSystem.Timer.Thread = nil
    end
end

function AutoSellSystem.Timer.SetInterval(seconds)
    local num = tonumber(seconds)
    if num and num >= 2 then
        AutoSellSystem.Timer.Interval = num
        return true
    end
    return false
end

function AutoSellSystem.Count.Start(target)
    if AutoSellSystem.Count.Enabled then return false end
    if not SellRemote then return false end
    
    if target and tonumber(target) and tonumber(target) > 10 then
        AutoSellSystem.Count.Target = tonumber(target)
    end
    
    AutoSellSystem.Count.Enabled = true
    
    AutoSellSystem.Count._thread = task.spawn(function()
        while AutoSellSystem.Count.Enabled do
            task.wait(AutoSellSystem.Count.CheckDelay)
            
            local current, max = getBagCount()
            if current >= AutoSellSystem.Count.Target and current > 0 then
                if tick() - AutoSellSystem.Count._lastSell > 3 then
                    AutoSellSystem.Count._lastSell = tick()
                    executeSell()
                    task.wait(1.5)
                end
            end
        end
    end)
    
    return true
end

function AutoSellSystem.Count.Stop()
    AutoSellSystem.Count.Enabled = false
    if AutoSellSystem.Count._thread then
        task.cancel(AutoSellSystem.Count._thread)
        AutoSellSystem.Count._thread = nil
    end
end

function AutoSellSystem.Count.SetTarget(count)
    local num = tonumber(count)
    if num and num > 10 then
        AutoSellSystem.Count.Target = num
        return true
    end
    return false
end

-- Init check & notif
if SellRemote then
    WindUI:Notify({
        Title = "Auto Sell Loaded ✓",
        Content = "Remote SellAllItems ditemukan! Path: " .. SellRemote:GetFullName(),
        Duration = 5,
        Icon = "check"
    })
else
    WindUI:Notify({
        Title = "Auto Sell Error",
        Content = "Remote SellAllItems TIDAK ditemukan! Cek console F9 untuk detail kandidat remote.",
        Duration = 8,
        Icon = "alert-triangle"
    })
end

-- === GUI CONTROLS ===
local SectionAutoSell = Tab3:Section({
    Title = "Auto Sell System",
    TextXAlignment = "Left",
    TextSize = 17,
})

local masterEnabled = false
Tab3:Toggle({
    Title = "Enable Auto Sell System",
    Desc = "Aktifkan untuk pakai semua mode auto sell",
    Icon = "coins",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        masterEnabled = state
        if not state then
            AutoSellSystem.Timer.Stop()
            AutoSellSystem.Count.Stop()
            WindUI:Notify({
                Title = "Auto Sell",
                Content = "Semua mode dimatikan",
                Duration = 4,
                Icon = "shield-off"
            })
        else
            if not SellRemote then
                WindUI:Notify({
                    Title = "Error",
                    Content = "Remote SellAllItems tidak ditemukan! Cek F9 untuk kandidat.",
                    Duration = 6,
                    Icon = "alert-triangle"
                })
                return false
            end
            WindUI:Notify({
                Title = "Auto Sell Active",
                Content = "Pilih mode Timer atau By Count di bawah",
                Duration = 4,
                Icon = "coins"
            })
        end
    end
})

Tab3:Button({
    Title = "Sell All Now (Manual)",
    Desc = "Jual semua ikan sekarang",
    Icon = "dollar-sign",
    Callback = function()
        if AutoSellSystem.SellOnce() then
            WindUI:Notify({
                Title = "Success",
                Content = "Sell All berhasil dipanggil!",
                Duration = 3
            })
        else
            WindUI:Notify({
                Title = "Gagal",
                Content = "Cek console F9 untuk error",
                Duration = 4,
                Icon = "alert-triangle"
            })
        end
    end
})

-- Timer Mode
local SectionTimer = Tab3:Section({
    Title = "Auto Sell Timer",
})

Tab3:Toggle({
    Title = "Sell Setiap X Detik",
    Desc = "Jual otomatis berkala",
    Icon = "timer",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if not masterEnabled then
            WindUI:Notify({Title = "Master Off", Content = "Nyalakan Enable Auto Sell dulu!", Duration = 4})
            return
        end
        if state then
            if AutoSellSystem.Timer.Start() then
                WindUI:Notify({
                    Title = "Timer ON",
                    Content = "Sell setiap " .. AutoSellSystem.Timer.Interval .. " detik",
                    Duration = 4
                })
            end
        else
            AutoSellSystem.Timer.Stop()
            WindUI:Notify({Title = "Timer OFF", Content = "Dimatikan", Duration = 3})
        end
    end
})

Tab3:Input({
    Title = "Interval (detik)",
    Desc = "Minimal 2 detik, rekomendasi 5-10",
    Value = "5",
    Placeholder = "5",
    Type = "Input",
    Callback = function(val)
        local num = tonumber(val)
        if num and num >= 2 then
            AutoSellSystem.Timer.SetInterval(num)
            WindUI:Notify({
                Title = "Interval Diubah",
                Content = "Sell setiap " .. num .. " detik",
                Duration = 3
            })
        else
            WindUI:Notify({Title = "Error", Content = "Minimal 2 detik", Duration = 4})
        end
    end
})

-- Count Mode
local SectionCount = Tab3:Section({
    Title = "Auto Sell Saat Kantong Penuh",
})

Tab3:Toggle({
    Title = "Sell Saat Ikan >= Target",
    Desc = "Otomatis jual kalau kantong mencapai batas",
    Icon = "package",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if not masterEnabled then
            WindUI:Notify({Title = "Master Off", Content = "Nyalakan Enable Auto Sell dulu!", Duration = 4})
            return
        end
        if state then
            if AutoSellSystem.Count.Start() then
                WindUI:Notify({
                    Title = "Count Mode ON",
                    Content = "Sell saat >= " .. AutoSellSystem.Count.Target .. " ikan",
                    Duration = 4
                })
            end
        else
            AutoSellSystem.Count.Stop()
            WindUI:Notify({Title = "Count Mode OFF", Content = "Dimatikan", Duration = 3})
        end
    end
})

Tab3:Input({
    Title = "Target Jumlah Ikan",
    Desc = "Contoh: 230-240 (sesuaikan max bag kamu)",
    Value = "235",
    Placeholder = "235",
    Type = "Input",
    Callback = function(val)
        local num = tonumber(val)
        if num and num > 10 then
            AutoSellSystem.Count.SetTarget(num)
            WindUI:Notify({
                Title = "Target Diubah",
                Content = "Sell saat kantong >= " .. num,
                Duration = 3
            })
        else
            WindUI:Notify({Title = "Error", Content = "Masukkan angka > 10", Duration = 4})
        end
    end
})

-- Debug button
Tab3:Button({
    Title = "Cek Status & Remote (F9)",
    Desc = "Print info lengkap ke console",
    Callback = function()
        if not SellRemote then
            warn("Remote SellAllItems BELUM DITEMUKAN! Cek kandidat di atas.")
        else
            warn("=== AUTO SELL STATUS ===")
            warn("Remote Path: " .. SellRemote:GetFullName())
            warn("Class: " .. SellRemote.ClassName)
            warn("Total Sell Dipanggil: " .. AutoSellSystem._totalSells)
            warn("Timer Aktif: " .. tostring(AutoSellSystem.Timer.Enabled) .. " | Interval: " .. AutoSellSystem.Timer.Interval .. "s")
            warn("Count Aktif: " .. tostring(AutoSellSystem.Count.Enabled) .. " | Target: " .. AutoSellSystem.Count.Target)
            local cur, max = getBagCount()
            warn("Kantong Saat Ini: " .. cur .. " / " .. (max > 0 and max or "???"))
        end
    end
})

local SectionBlatant = Tab3:Section({
    Title = "Show Ping Notification",
    TextXAlignment = "Left",
    TextSize = 17,
})

Tab3:Button({
    Title = "Notif Counter + Ping/CPU",
    Desc = "Show Realtime",
    Icon = "bell-ring",
    Callback = function()
        pcall(function()
            -- GANTI DENGAN LINK RAW PASTEBIN/GITHUB KAMU
            WindUI:Notify({Title = "Segera Datang!", Content = "Comming Soon", Duration = 3})
        end)
    end
})


local Section = Tab3:Section({ 
    Title = "Opsional",
    TextXAlignment = "Left",
    TextSize = 17,
})

local ToggleCatch = Tab3:Toggle({
    Title = "Instant Catch",
    Desc = "Get fish straight away",
    Icon = "fish",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        _G.InstantCatch = state

        if state then
            print("✅ Instant Catch ON")
            if _loopRunning then return end
            _loopRunning = true

            task.spawn(function()
                while _G.InstantCatch do
                    local remote = findRemote(REMOTE_NAME)
                    if remote then
                        local success, err = tryFire(remote)
                        if success then
                            print("🎣 Instant catch success!")
                        else
                            warn("❌ error:", err)
                        end
                    else
                        warn("⚠️ Remote '" .. REMOTE_NAME .. "' tidak ditemukan. Jalankan scanner dulu.")
                    end
                    task.wait(TRY_INTERVAL)
                end
                _loopRunning = false
                print("❌ Instant Catch OFF")
            end)
        else
            print("❌ Instant Catch is turned off")
        end
    end
})

local Tab4 = Window:Tab({
    Title = "Teleport",
    Icon = "map-pin",
})

local Section = Tab4:Section({ 
    Title = "Island",
    TextXAlignment = "Left",
    TextSize = 17,
})

local Dropdown = Tab4:Dropdown({
    Title = "Select Location",
    Values = {
        "Ancient Jungle",
        "Ancient Ruin",
        "Christmas Cave",
        "Christmas Island",
        "Classic Island",
        "Coral Reefs",
        "Crater Island",
        "Crystal Depths",
        "Enchant Room",
        "Esoteric Depths",
        "Esoteric Island",
        "Fisherman Island",
        "Iron Cavern",
        "Kohana",
        "Kohana Volcano",
        "Leviathan Den",
        "Lost Isle",
        "Pirate Treasure Room",
        "Sacred Temple",
        "Secret Passage",
        "Sysiphus Statue",
        "The Iron Cafe",
        "Treasure Room",
        "Tropical Grove",
        "Underground Cellar",
        "Weather Machine"
    },
    Callback = function(Value)
        local Locations = {
            ["Ancient Jungle"]       = Vector3.new(1467.8480224609375, 7.447117328643799, -327.5971984863281),
            ["Ancient Ruin"]         = Vector3.new(6045.40234375, -588.600830078125, 4608.9375),
            ["Christmas Cave"]       = Vector3.new(535.279724121093750, -580.581359863281250, 8900.060546875000000),
            ["Christmas Island"]     = Vector3.new(1130.576904, 23.854950, 1554.231567),
            ["Classic Island"]       = Vector3.new(1253.974853515625, 9.999999046325684, 2816.7646484375),
            ["Coral Reefs"]          = Vector3.new(-2921.858154296875, 3.249999761581421, 2083.2978515625),
            ["Crater Island"]        = Vector3.new(1078.454345703125, 5.0720038414001465, 5099.396484375),
            ["Crystal Depths"]       = Vector3.new(5834.966, -905.620, 15350.285),
            ["Enchant Room"]         = Vector3.new(3221, -1303, 1406),
            ["Esoteric Depths"]      = Vector3.new(3224.075927734375, -1302.85498046875, 1404.9346923828125),
            ["Esoteric Island"]      = Vector3.new(1990, 5, 1398),
            ["Fisherman Island"]     = Vector3.new(92.80695343017578, 9.531265258789062, 2762.082275390625),
            ["Iron Cavern"]          = Vector3.new(-8881.52734375, -581.7500610351562, 156.1653289794922),
            ["Kohana"]               = Vector3.new(-643.3051147460938, 16.03544807434082, 622.3605346679688),
            ["Kohana Volcano"]       = Vector3.new(-572.0244750976562, 39.4923210144043, 112.49259185791016),
            ["Leviathan Den"]        = Vector3.new(3473.000, -287.843, 3472.000),
            ["Lost Isle"]            = Vector3.new(-3701.1513671875, 5.425841808319092, -1058.9107666015625),
            ["Pirate Treasure Room"] = Vector3.new(3308.925, -303.268, 3030.480),
            ["Sacred Temple"]        = Vector3.new(1476.30810546875, -21.8499755859375, -630.8220825195312),
            ["Secret Passage"]       = Vector3.new(3447.000, -287.845, 3401.000),
            ["Sysiphus Statue"]      = Vector3.new(-3656.56201171875, -134.5314178466797, -964.3167724609375),
            ["The Iron Cafe"]        = Vector3.new(-8642.7265625, -547.5001831054688, 159.8160400390625),
            ["Treasure Room"]        = Vector3.new(-3600, -267, -1575),
            ["Tropical Grove"]       = Vector3.new(-2091, 6, 3703),
            ["Underground Cellar"]   = Vector3.new(2162.577392578125, -91.1981430053711, -725.591552734375),
            ["Weather Machine"]      = Vector3.new(-1508, 6, 1895),
        }

        local pos = Locations[Value]
        if not pos then
            WindUI:Notify({
                Title = "Teleport Error",
                Content = "Lokasi '" .. Value .. "' belum terdaftar!",
                Duration = 5,
                Icon = "alert-triangle"
            })
            return
        end

        local player = game.Players.LocalPlayer
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            -- Offset Y lebih tinggi untuk area deep/underground biar aman
            local offsetY = (Value:find("Depths") or Value:find("Den") or Value:find("Treasure") or Value:find("Secret") or Value:find("Passage")) and 12 or 5
            char.HumanoidRootPart.CFrame = CFrame.new(pos + Vector3.new(0, offsetY, 0))
            
            WindUI:Notify({
                Title = "Teleport Success",
                Content = "Warped to: " .. Value,
                Duration = 4,
                Icon = "map-pin"
            })
        else
            WindUI:Notify({
                Title = "Teleport Failed",
                Content = "Character atau HumanoidRootPart tidak ditemukan!",
                Duration = 5,
                Icon = "alert-triangle"
            })
        end
    end
})
-- =================================================
--     ⭐ TELEPORT TO PLAYER + AUTO REFRESH LIST ⭐
--     Dynamic dropdown player list (update join/leave)
--     Paste SETELAH Auto Buy Weather di Tab4
-- =================================================

local SectionPlayerTP = Tab4:Section({
    Title = "Teleport to Player",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE TELEPORT TO PLAYER =====================
local TeleportToPlayer = {}

local PlayersService = game:GetService("Players")
local localPlayer = PlayersService.LocalPlayer

-- Table untuk simpan list player names (dynamic)
local playerList = {}

-- Fungsi update dropdown
local currentDropdown = nil  -- nanti kita simpan reference dropdown-nya

local function updatePlayerList()
    playerList = {}
    for _, plr in ipairs(PlayersService:GetPlayers()) do
        if plr ~= localPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            table.insert(playerList, plr.Name)
        end
    end
    
    table.sort(playerList)  -- urut alfabet biar rapi
    
    -- Kalau dropdown udah dibuat, update values-nya
    if currentDropdown then
        currentDropdown:UpdateValues(playerList)  -- Asumsi WindUI punya method UpdateValues atau refresh
        -- Kalau WindUI gak punya, kita recreate dropdown (lihat note bawah)
    end
    
    print("[PlayerTP] List updated: " .. #playerList .. " players online")
end

-- Initial populate
updatePlayerList()

-- Auto refresh on player join/leave
PlayersService.PlayerAdded:Connect(function(plr)
    task.wait(1)  -- tunggu char load
    updatePlayerList()
end)

PlayersService.PlayerRemoving:Connect(function(plr)
    updatePlayerList()
end)

-- Fungsi teleport
function TeleportToPlayer.TeleportTo(playerName)
    local target = PlayersService:FindFirstChild(playerName)
    if not target or not target.Character then 
        warn("[Teleport] Player " .. playerName .. " tidak ditemukan atau no char!")
        return 
    end

    local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
    local myChar = localPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")

    if targetHRP and myHRP then
        myHRP.CFrame = targetHRP.CFrame + Vector3.new(0, 3, 0)  -- di atas sedikit
        WindUI:Notify({
            Title = "Teleport Success",
            Content = "Teleported to: " .. playerName,
            Duration = 4,
            Icon = "user-check"
        })
        print("[Teleport] 🚀 Berhasil ke: " .. playerName)
    else
        WindUI:Notify({
            Title = "Teleport Failed",
            Content = "HumanoidRootPart tidak ditemukan!",
            Duration = 5,
            Icon = "alert-triangle"
        })
        warn("[Teleport] ❌ Gagal, HRP tidak ditemukan.")
    end
end

-- ===================== GUI CONTROLS =====================

-- Dropdown dynamic player list
currentDropdown = Tab4:Dropdown({
    Title = "Select Player",
    Values = playerList,  -- initial empty atau populated
    Callback = function(selectedName)
        if selectedName and selectedName ~= "" then
            TeleportToPlayer.TeleportTo(selectedName)
        end
    end
})

-- Optional: Button manual refresh (kalau auto gak cukup cepet)
Tab4:Button({
    Title = "Refresh Player List",
    Desc = "Update manual kalau dropdown gak sync",
    Callback = function()
        updatePlayerList()
        WindUI:Notify({
            Title = "Player List",
            Content = "Refreshed! " .. #playerList .. " players detected",
            Duration = 3
        })
    end
})
-- =================================================
--     ⭐ EVENT TELEPORT DYNAMIC (AUTO HUNT) ⭐
--     Smart detect & auto TP to Shark/Worm/Megalo/Ghost events
--     Paste SETELAH Teleport to Player di Tab4
-- =================================================

local SectionEventTP = Tab4:Section({
    Title = "Dynamic Event Teleport",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE EVENT TELEPORT DYNAMIC =====================
-- (Full module kamu inline biar gak perlu loadstring terpisah)
local EventTeleport = {}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

EventTeleport.Events = {
    ["Shark Hunt"] = {
        Vector3.new(1.64999, -1.3500, 2095.72),
        Vector3.new(1369.94, -1.3500, 930.125),
        Vector3.new(-1585.5, -1.3500, 1242.87),
        Vector3.new(-1896.8, -1.3500, 2634.37),
    },

    ["Worm Hunt"] = {
        Vector3.new(2190.85, -1.3999, 97.5749),
        Vector3.new(-2450.6, -1.3999, 139.731),
        Vector3.new(-267.47, -1.3999, 5188.53),
    },

    ["Megalodon Hunt"] = {
        Vector3.new(-1076.3, -1.3999, 1676.19),
        Vector3.new(-1191.8, -1.3999, 3597.30),
        Vector3.new(412.700, -1.3999, 4134.39),
    },

    ["Ghost Shark Hunt"] = {
        Vector3.new(489.558, -1.3500, 25.4060),
        Vector3.new(-1358.2, -1.3500, 4100.55),
        Vector3.new(627.859, -1.3500, 3798.08),
    },

    ["Treasure Hunt"] = nil, -- no static coords, admin spawn
}

EventTeleport.SearchRadius = 25
EventTeleport.ScanInterval = 2.0
EventTeleport.HeightOffset = 15
EventTeleport.MaxPartsToCheck = 500
EventTeleport.RequireEventSpawned = true
EventTeleport.CacheValidPosition = true
EventTeleport.CacheDuration = 5
EventTeleport.TeleportRadius = 50

local running = false
local currentEventName = nil
local scanCoroutine = nil
local lastValidPosition = nil
local lastValidTime = 0
local eventDetectedOnce = false
local cachedParts = {}
local lastCacheUpdate = 0
local cacheUpdateInterval = 5

local function safeCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

local function getHRP()
    local char = LocalPlayer.Character
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function updatePartCache()
    local now = tick()
    if now - lastCacheUpdate < cacheUpdateInterval then return end
    lastCacheUpdate = now
    cachedParts = {}
    local count = 0
    for _, inst in ipairs(Workspace:GetDescendants()) do
        if inst:IsA("BasePart") and inst.Parent and inst.Parent.Name ~= "Terrain" then
            local isPlayerPart = Players:GetPlayerFromCharacter(inst.Parent) or Players:GetPlayerFromCharacter(inst.Parent.Parent)
            if not isPlayerPart then
                table.insert(cachedParts, inst)
                count = count + 1
                if count >= EventTeleport.MaxPartsToCheck * 2 then break end
            end
        end
    end
end

local function findNearbyObject(centerPos, radius)
    local bestPart, bestDist = nil, math.huge
    local radiusSq = radius * radius

    -- Fast: GetPartBoundsInBox
    if Workspace.GetPartBoundsInBox then
        local ok, parts = pcall(Workspace.GetPartBoundsInBox, Workspace, CFrame.new(centerPos), Vector3.new(radius*2, radius*2, radius*2))
        if ok and parts then
            for _, p in ipairs(parts) do
                if p:IsA("BasePart") then
                    local distSq = (p.Position - centerPos).Magnitude^2
                    if distSq <= radiusSq and distSq < bestDist then
                        bestDist = distSq
                        bestPart = p
                    end
                end
            end
            if bestPart then return bestPart end
        end
    end

    updatePartCache()
    local checked = 0
    for _, part in ipairs(cachedParts) do
        if part and part.Parent then
            local distSq = (part.Position - centerPos).Magnitude^2
            if distSq <= radiusSq and distSq < bestDist then
                bestDist = distSq
                bestPart = part
            end
            checked = checked + 1
            if checked >= EventTeleport.MaxPartsToCheck then break end
        end
    end
    return bestPart
end

local function resolveActivePosition(eventName)
    local coords = EventTeleport.Events[eventName]
    if not coords or #coords == 0 then return nil, false end

    if EventTeleport.CacheValidPosition and lastValidPosition and (tick() - lastValidTime < EventTeleport.CacheDuration) then
        return lastValidPosition, true
    end

    for _, coord in ipairs(coords) do
        local part = findNearbyObject(coord, EventTeleport.SearchRadius)
        if part then
            local safePos = part.Position + Vector3.new(0, EventTeleport.HeightOffset, 0)
            lastValidPosition = safePos
            lastValidTime = tick()
            return safePos, true
        end
    end

    if EventTeleport.RequireEventSpawned then
        lastValidPosition = nil
        lastValidTime = 0
        return nil, false
    else
        -- Fallback closest coord
        local hrp = getHRP()
        if hrp then
            local best, minDistSq = nil, math.huge
            for _, coord in ipairs(coords) do
                local distSq = (hrp.Position - coord).Magnitude^2
                if distSq < minDistSq then minDistSq = distSq best = coord end
            end
            if best then return best + Vector3.new(0, EventTeleport.HeightOffset, 0), false end
        end
        return coords[1] + Vector3.new(0, EventTeleport.HeightOffset, 0), false
    end
end

local function doTeleportToPos(pos)
    if not pos then return false end
    local hrp = getHRP()
    if not hrp then return false end

    local distSq = (hrp.Position - pos).Magnitude^2
    if distSq <= (EventTeleport.TeleportRadius ^ 2) then return false end

    return pcall(function()
        hrp.CFrame = CFrame.new(pos)
    end)
end

function EventTeleport.TeleportNow(eventName)
    local pos, isSpawned = resolveActivePosition(eventName)
    if not pos or (EventTeleport.RequireEventSpawned and not isSpawned) then return false end
    return doTeleportToPos(pos)
end

function EventTeleport.Start(eventName)
    if running or not EventTeleport.Events[eventName] then return false end
    running = true
    currentEventName = eventName
    eventDetectedOnce = false
    lastValidPosition = nil
    lastValidTime = 0

    scanCoroutine = task.spawn(function()
        while running do
            local pos, isSpawned = resolveActivePosition(currentEventName)
            if pos then
                if EventTeleport.RequireEventSpawned then
                    if isSpawned then
                        if not eventDetectedOnce then eventDetectedOnce = true end
                        doTeleportToPos(pos)
                    end
                else
                    doTeleportToPos(pos)
                end
            end
            task.wait(EventTeleport.ScanInterval)
        end
    end)
    return true
end

function EventTeleport.Stop()
    running = false
    currentEventName = nil
    eventDetectedOnce = false
    lastValidPosition = nil
    lastValidTime = 0
    cachedParts = {}
    if scanCoroutine then task.cancel(scanCoroutine) scanCoroutine = nil end
    return true
end

function EventTeleport.GetEventNames()
    local list = {}
    for name in pairs(EventTeleport.Events) do table.insert(list, name) end
    table.sort(list)
    return list
end

-- ===================== GUI CONTROLS =====================

local DropdownEvent = Tab4:Dropdown({
    Title = "Select Event",
    Values = EventTeleport.GetEventNames(),
    Callback = function(Value)
        WindUI:Notify({
            Title = "Event Selected",
            Content = "Selected: " .. Value .. "\nToggle auto TP to start hunting!",
            Duration = 4,
            Icon = "map-pin"
        })
    end
})

local ToggleAutoEventTP = Tab4:Toggle({
    Title = "Auto Teleport to Event",
    Desc = "Auto TP ke spawn event (smart detect object)",
    Icon = "zap",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        local selected = DropdownEvent.Value or "Shark Hunt"  -- default kalau kosong
        if state then
            if EventTeleport.Start(selected) then
                WindUI:Notify({
                    Title = "Event Auto TP Activated",
                    Content = "Hunting: " .. selected .. " | Scan every " .. EventTeleport.ScanInterval .. "s",
                    Duration = 5,
                    Icon = "zap"
                })
            else
                WindUI:Notify({Title = "Error", Content = "Event tidak valid atau no coords!", Duration = 4})
            end
        else
            EventTeleport.Stop()
            WindUI:Notify({
                Title = "Event Auto TP Stopped",
                Content = "Auto hunting dimatikan",
                Duration = 4,
                Icon = "zap-off"
            })
        end
    end
})

-- Button manual TP sekali
Tab4:Button({
    Title = "Teleport Now (Manual)",
    Desc = "TP sekali ke event terpilih (kalau spawned)",
    Callback = function()
        local selected = DropdownEvent.Value
        if selected and EventTeleport.TeleportNow(selected) then
            WindUI:Notify({
                Title = "Manual TP Success",
                Content = "Warped to " .. selected .. " event!",
                Duration = 3
            })
        else
            WindUI:Notify({
                Title = "Manual TP Failed",
                Content = "Event belum spawn atau no coords!",
                Duration = 4
            })
        end
    end
})

local Section = Tab4:Section({ 
    Title = "fishing spot",
    TextXAlignment = "Left",
    TextSize = 17,
})

local Dropdown = Tab4:Dropdown({
    Title = "Select Location",
    Values = {"Spawn", "Konoha", "Coral Refs", "Volcano", "Sysyphus Statue"},
    Callback = function(Value)
        local Locations = {
            ["Spawn"] = Vector3.new(33, 9, 2810),
            ["Konoha"] = Vector3.new(-603, 3, 719),
            ["Coral Refs"] = Vector3.new(-2855, 47, 1996),
            ["Volcano"] = Vector3.new(-632, 55, 197),
            ["Sysyphus Statue"] = Vector3.new(-3693,-136,-1045),
        }

        local Player = game.Players.LocalPlayer
        if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
            Player.Character.HumanoidRootPart.CFrame = CFrame.new(Locations[Value])
        end
    end
})


local Tab5 = Window:Tab({
    Title = "Settings",
    Icon = "settings",
})

-- =================================================
--     HIDE STATS IDENTIFIER MODULE (OVERHEAD FAKE)
--     Ganti nama/level + script name shimmer
--     Paste SETELAH bagian akhir Tab5 (Settings)
-- =================================================

local SectionHideStats = Tab5:Section({
    Title = "Hide Stats Identifier",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE HIDE STATS =====================
local HideStatsModule = {}

-- Settings (bisa diubah manual di code)
local HideStatsEnabled = false
local FakeName = "HALO STAF HAHAHA"
local FakeLevel = "-999"
local ScriptName = "SCRIPT PAHAJIHUB"

local OriginalTexts = {}
local ActiveGradientThreads = {}

-- Warna shimmer gradient orange-putih
local ShimmerColors = {
    Color3.fromRGB(255, 140, 0),
    Color3.fromRGB(255, 180, 50),
    Color3.fromRGB(255, 220, 150),
    Color3.fromRGB(255, 255, 255),
    Color3.fromRGB(255, 220, 150),
    Color3.fromRGB(255, 180, 50),
    Color3.fromRGB(255, 140, 0),
}

-- Fungsi shimmer gradient bergerak
local function createMovingGradient(label)
    if not label or not label:IsA("TextLabel") then return end
    
    local oldGradient = label:FindFirstChild("ShimmerGradient")
    if oldGradient then oldGradient:Destroy() end
    
    local gradient = Instance.new("UIGradient")
    gradient.Name = "ShimmerGradient"
    gradient.Parent = label
    
    local colorKeypoints = {}
    local basePattern = {
        {0.00, Color3.fromRGB(255, 140, 0)},
        {0.10, Color3.fromRGB(255, 160, 30)},
        {0.20, Color3.fromRGB(255, 200, 100)},
        {0.30, Color3.fromRGB(255, 255, 255)},
        {0.40, Color3.fromRGB(255, 200, 100)},
        {0.50, Color3.fromRGB(255, 160, 30)},
        {0.60, Color3.fromRGB(255, 140, 0)},
        {0.70, Color3.fromRGB(255, 160, 30)},
        {0.80, Color3.fromRGB(255, 200, 100)},
        {0.90, Color3.fromRGB(255, 255, 255)},
        {1.00, Color3.fromRGB(255, 140, 0)},
    }
    
    for _, data in ipairs(basePattern) do
        table.insert(colorKeypoints, ColorSequenceKeypoint.new(data[1], data[2]))
    end
    
    gradient.Color = ColorSequence.new(colorKeypoints)
    
    local threadId = tostring(label)
    ActiveGradientThreads[threadId] = true
    
    spawn(function()
        local offset = 0
        while label and label.Parent and ActiveGradientThreads[threadId] do
            offset = offset + 0.015
            if offset >= 1 then offset = 0 end
            gradient.Offset = Vector2.new(offset, 0)
            wait(0.02)
        end
    end)
    
    return gradient
end

-- Clone TextLabel untuk script name
local function createScriptNameLabel(nameLabel, billboard)
    if not nameLabel or not billboard then return end
    
    local existingFrame = billboard:FindFirstChild("LynxFrame")
    if existingFrame then return existingFrame end
    
    local nameFrame = nameLabel.Parent
    if not nameFrame or not nameFrame:IsA("Frame") then return end
    
    local originalNamePos = nameFrame.Position
    nameFrame.Position = UDim2.new(
        originalNamePos.X.Scale,
        originalNamePos.X.Offset,
        originalNamePos.Y.Scale + 0.25,
        originalNamePos.Y.Offset
    )
    
    local lynxFrame = Instance.new("Frame")
    lynxFrame.Name = "LynxFrame"
    lynxFrame.Size = nameFrame.Size
    lynxFrame.Position = originalNamePos
    lynxFrame.BackgroundTransparency = 1
    lynxFrame.Parent = billboard
    
    local scriptLabel = nameLabel:Clone()
    scriptLabel.Name = "LynxLabel"
    scriptLabel.Text = ScriptName
    scriptLabel.TextScaled = true
    scriptLabel.Font = Enum.Font.GothamBold
    scriptLabel.TextStrokeTransparency = 0.5
    scriptLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    scriptLabel.Parent = lynxFrame
    
    createMovingGradient(scriptLabel)
    
    return lynxFrame
end

-- Hapus semua script name labels
local function removeAllScriptNames()
    local character = LocalPlayer.Character
    if not character then return end
    
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local overhead = hrp:FindFirstChild("Overhead")
    if not overhead then return end
    
    local lynxFrame = overhead:FindFirstChild("LynxFrame")
    if lynxFrame then
        for threadId, _ in pairs(ActiveGradientThreads) do
            ActiveGradientThreads[threadId] = nil
        end
        
        local nameLabel = overhead:FindFirstChild("Header", true)
        if nameLabel then
            local nameFrame = nameLabel.Parent
            if nameFrame and nameFrame:IsA("Frame") then
                local currentPos = nameFrame.Position
                nameFrame.Position = UDim2.new(
                    currentPos.X.Scale,
                    currentPos.X.Offset,
                    currentPos.Y.Scale - 0.25,
                    currentPos.Y.Offset
                )
            end
        end
        
        lynxFrame:Destroy()
    end
end

-- Update nama & level overhead
local function updateStats()
    if not HideStatsEnabled then 
        removeAllScriptNames()
        return 
    end
    
    local character = LocalPlayer.Character
    if not character then return end
    
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local overhead = hrp:FindFirstChild("Overhead")
    if not overhead or not overhead:IsA("BillboardGui") then return end
    
    for _, obj in pairs(overhead:GetDescendants()) do
        if obj:IsA("TextLabel") then
            local fullPath = obj:GetFullName()
            
            if not OriginalTexts[fullPath] then
                OriginalTexts[fullPath] = obj.Text
            end
            
            local originalText = OriginalTexts[fullPath]
            
            if originalText and originalText ~= "" then
                if obj.Name == "Header" then
                    if not overhead:FindFirstChild("LynxFrame") then
                        createScriptNameLabel(obj, overhead)
                    end
                    obj.Text = FakeName
                elseif string.find(string.lower(originalText), "lvl") then
                    obj.Text = string.gsub(originalText, "%d+", FakeLevel)
                end
            end
        end
    end
end

-- Auto-update loop
local updateLoop
local function startUpdateLoop()
    if updateLoop then return end
    updateLoop = true
    spawn(function()
        while updateLoop do
            if HideStatsEnabled then
                updateStats()
            end
            wait(0.2)
        end
    end)
end

-- PUBLIC FUNCTIONS
function HideStatsModule.Enable()
    HideStatsEnabled = true
    startUpdateLoop()
    updateStats()
end

function HideStatsModule.Disable()
    HideStatsEnabled = false
    
    -- Restore original
    for path, originalText in pairs(OriginalTexts) do
        local obj = game
        for part in string.gmatch(path, "[^.]+") do
            obj = obj:FindFirstChild(part)
            if not obj then break end
        end
        if obj and obj:IsA("TextLabel") then
            obj.Text = originalText
        end
    end
    
    removeAllScriptNames()
end

-- ===================== GUI TOGGLE =====================

local ToggleHideStats = Tab5:Toggle({
    Title = "Hide Stats Identifier",
    Desc = "Ganti nama/level + script name shimmer (client-side)",
    Icon = "user-x",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            HideStatsModule.Enable()
            WindUI:Notify({
                Title = "Hide Stats Activated",
                Content = "Nama: " .. FakeName .. " | Level: " .. FakeLevel .. " + shimmer",
                Duration = 5,
                Icon = "user-x"
            })
        else
            HideStatsModule.Disable()
            WindUI:Notify({
                Title = "Hide Stats Deactivated",
                Content = "Stats kembali normal",
                Duration = 5,
                Icon = "user-check"
            })
        end
    end
})

-- Optional: Input custom fake name & level
local InputFakeName = Tab5:Input({
    Title = "Fake Name",
    Desc = "Ganti nama overhead (default: TAKUT STAFF BANG)",
    Value = FakeName,
    Placeholder = "Masukkan nama fake...",
    InputIcon = "user",
    Type = "Input",
    Callback = function(val)
        if val and val ~= "" then
            FakeName = val
            if HideStatsEnabled then updateStats() end
            WindUI:Notify({
                Title = "Fake Name Updated",
                Content = "Nama diubah ke: " .. val,
                Duration = 3
            })
        end
    end
})

local InputFakeLevel = Tab5:Input({
    Title = "Fake Level",
    Desc = "Ganti level overhead (default: -999)",
    Value = FakeLevel,
    Placeholder = "-999",
    InputIcon = "star",
    Type = "Input",
    Callback = function(val)
        if val then
            FakeLevel = val
            if HideStatsEnabled then updateStats() end
            WindUI:Notify({
                Title = "Fake Level Updated",
                Content = "Level diubah ke: " .. val,
                Duration = 3
            })
        end
    end
})

-- Respawn & DescendantAdded handler (persis sama)
LocalPlayer.CharacterAdded:Connect(function(character)
    OriginalTexts = {}
    ActiveGradientThreads = {}
    wait(1)
    if HideStatsEnabled then
        updateStats()
    end
end)

if LocalPlayer.Character then
    LocalPlayer.Character.DescendantAdded:Connect(function(descendant)
        if HideStatsEnabled and descendant:IsA("BillboardGui") then
            wait(0.1)
            updateStats()
        end
    end)
end

-- Initial setup
if LocalPlayer.Character then
    wait(1)
    if HideStatsEnabled then
        updateStats()
    end
end

local Toggle = Tab5:Toggle({
    Title = "AntiAFK",
    Desc = "Prevent Roblox from kicking you when idle",
    Icon = "shield",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        _G.AntiAFK = state
        local VirtualUser = game:GetService("VirtualUser")
        local player = game:GetService("Players").LocalPlayer

        task.spawn(function()
            while _G.AntiAFK do
                task.wait(60)
                pcall(function()
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.new())
                end)
            end
        end)

        if state then
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "AntiAFK loaded!",
                Text = "BAHLIL ANJING",
                Button1 = "Okey",
                Duration = 5
            })
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "AntiAFK Disabled",
                Text = "Stopped AntiAFK",
                Duration = 3
            })
        end
    end
})

local Toggle = Tab5:Toggle({
    Title = "Auto Reconnect",
    Desc = "Automatic reconnect if disconnected",
    Icon = "plug-zap",
    Default = false,
    Callback = function(state)
        _G.AutoReconnect = state
        if state then
            task.spawn(function()
                while _G.AutoReconnect do
                    task.wait(2)

                    local reconnectUI = game:GetService("CoreGui"):FindFirstChild("RobloxPromptGui")
                    if reconnectUI then
                        local prompt = reconnectUI:FindFirstChild("promptOverlay")
                        if prompt then
                            local button = prompt:FindFirstChild("ButtonPrimary")
                            if button and button.Visible then
                                firesignal(button.MouseButton1Click)
                            end
                        end
                    end
                end
            end)
        end
    end
})
-- =================================================
--     DISABLE EXTRAS (Small Notif + Dive Effects)
--     Paste SETELAH Toggle Auto Reconnect di Tab5
-- =================================================

local SectionExtras = Tab5:Section({
    Title = "Disable Visual Extras",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE DISABLE EXTRAS =====================
local DisableExtras = {}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local VFXFolder = ReplicatedStorage:WaitForChild("VFX")

local activeSmallNotif = false
local activeSkinEffect = false

-- Small Notification Disabler
local function disableNotifications()
    if not player or not player:FindFirstChild("PlayerGui") then return end
    local gui = player.PlayerGui
    local existing = gui:FindFirstChild("Small Notification")
    if existing then
        existing:Destroy()
    end
end

-- Skin Dive Effect Disabler
local function disableDiveEffects()
    for _, child in pairs(VFXFolder:GetChildren()) do
        if child.Name:match("Dive$") then
            child:Destroy()
        end
    end
end

-- Small Notification Controls
function DisableExtras.StartSmallNotification()
    if activeSmallNotif then return end
    activeSmallNotif = true

    -- Immediate cleanup
    disableNotifications()

    -- Heartbeat loop
    local notifConn = RunService.Heartbeat:Connect(function()
        if activeSmallNotif then
            disableNotifications()
        end
    end)

    -- Child added detection
    local childAddedConn = player.PlayerGui.ChildAdded:Connect(function(child)
        if activeSmallNotif and child.Name == "Small Notification" then
            child:Destroy()
        end
    end)

    -- Cleanup saat stop nanti
    DisableExtras._notifCleanup = function()
        notifConn:Disconnect()
        childAddedConn:Disconnect()
    end
end

function DisableExtras.StopSmallNotification()
    activeSmallNotif = false
    if DisableExtras._notifCleanup then
        DisableExtras._notifCleanup()
        DisableExtras._notifCleanup = nil
    end
end

-- Skin Effect Controls
function DisableExtras.StartSkinEffect()
    if activeSkinEffect then return end
    activeSkinEffect = true

    -- Immediate cleanup
    disableDiveEffects()

    -- Heartbeat loop
    local effectConn = RunService.Heartbeat:Connect(function()
        if activeSkinEffect then
            disableDiveEffects()
        end
    end)

    -- Child added detection
    local vfxAddedConn = VFXFolder.ChildAdded:Connect(function(child)
        if activeSkinEffect and child.Name:match("Dive$") then
            child:Destroy()
        end
    end)

    -- Cleanup saat stop
    DisableExtras._effectCleanup = function()
        effectConn:Disconnect()
        vfxAddedConn:Disconnect()
    end
end

function DisableExtras.StopSkinEffect()
    activeSkinEffect = false
    if DisableExtras._effectCleanup then
        DisableExtras._effectCleanup()
        DisableExtras._effectCleanup = nil
    end
end

-- ===================== GUI TOGGLES =====================

local ToggleSmallNotif = Tab5:Toggle({
    Title = "Disable Small Notifications",
    Desc = "Hilangkan notifikasi kecil di layar",
    Icon = "bell-off",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            DisableExtras.StartSmallNotification()
            WindUI:Notify({
                Title = "Extras Disabled",
                Content = "Small Notifications dimatikan permanen",
                Duration = 4,
                Icon = "bell-off"
            })
        else
            DisableExtras.StopSmallNotification()
            WindUI:Notify({
                Title = "Extras Restored",
                Content = "Small Notifications diaktifkan kembali",
                Duration = 4
            })
        end
    end
})

local ToggleDiveEffect = Tab5:Toggle({
    Title = "Disable Dive Skin Effects",
    Desc = "Hilangkan efek visual dive dari skin",
    Icon = "waves",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            DisableExtras.StartSkinEffect()
            WindUI:Notify({
                Title = "Extras Disabled",
                Content = "Dive Skin Effects dimatikan",
                Duration = 4,
                Icon = "waves"
            })
        else
            DisableExtras.StopSkinEffect()
            WindUI:Notify({
                Title = "Extras Restored",
                Content = "Dive Skin Effects diaktifkan kembali",
                Duration = 4
            })
        end
    end
})
-- =================================================
--     ⭐ FPS BOOSTER MODULE (OPTIMIZED FOR FISH IT) ⭐
--     Disable shadows, fog, particles, water waves, etc.
--     Paste SETELAH Skin Animation Replacer di Tab5
-- =================================================

local SectionFPS = Tab5:Section({
    Title = "FPS Booster (Performance Mode)",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE FPS BOOSTER =====================
local FPSBooster = {}
FPSBooster.Enabled = false

local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Terrain = workspace:FindFirstChildOfClass("Terrain")

local originalStates = {
    reflectance = {},
    transparency = {},
    lighting = {},
    effects = {},
    waterProperties = {}
}

local newObjectConnection = nil

local function optimizeObject(obj)
    if not FPSBooster.Enabled then return end
    
    pcall(function()
        if obj:IsA("BasePart") then
            if not originalStates.reflectance[obj] then
                originalStates.reflectance[obj] = obj.Reflectance
            end
            obj.Reflectance = 0
            obj.CastShadow = false
        end
        
        if obj:IsA("Decal") or obj:IsA("Texture") then
            if not originalStates.transparency[obj] then
                originalStates.transparency[obj] = obj.Transparency
            end
            obj.Transparency = 1
        end
        
        if obj:IsA("SurfaceAppearance") then
            obj:Destroy()
        end
        
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or
           obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then
            obj.Enabled = false
        end
    end)
end

local function restoreObject(obj)
    pcall(function()
        if obj:IsA("BasePart") and originalStates.reflectance[obj] then
            obj.Reflectance = originalStates.reflectance[obj]
            obj.CastShadow = true
        end
        
        if (obj:IsA("Decal") or obj:IsA("Texture")) and originalStates.transparency[obj] then
            obj.Transparency = originalStates.transparency[obj]
        end
        
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or
           obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then
            obj.Enabled = true
        end
    end)
end

function FPSBooster.Enable()
    if FPSBooster.Enabled then return false, "Already enabled" end
    
    FPSBooster.Enabled = true
    
    -- Optimize existing objects
    for _, obj in ipairs(workspace:GetDescendants()) do
        optimizeObject(obj)
    end
    
    -- Optimize Terrain Water (no waves/reflections)
    if Terrain then
        pcall(function()
            originalStates.waterProperties = {
                WaterReflectance = Terrain.WaterReflectance,
                WaterWaveSize = Terrain.WaterWaveSize,
                WaterWaveSpeed = Terrain.WaterWaveSpeed
            }
            Terrain.WaterWaveSize = 0
            Terrain.WaterWaveSpeed = 0
            Terrain.WaterReflectance = 0
        end)
    end
    
    -- Optimize Lighting
    originalStates.lighting = {
        GlobalShadows = Lighting.GlobalShadows,
        FogEnd = Lighting.FogEnd,
        FogStart = Lighting.FogStart
    }
    Lighting.GlobalShadows = false
    Lighting.FogStart = 0
    Lighting.FogEnd = 1000000
    
    -- Disable Post Effects
    for _, effect in ipairs(Lighting:GetChildren()) do
        if effect:IsA("PostEffect") then
            originalStates.effects[effect] = effect.Enabled
            effect.Enabled = false
        end
    end
    
    -- Set minimum quality
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    
    -- Hook new objects
    newObjectConnection = workspace.DescendantAdded:Connect(function(obj)
        if FPSBooster.Enabled then
            task.wait(0.1)
            optimizeObject(obj)
        end
    end)
    
    return true, "FPS Booster enabled - expect +20-60 FPS in Fish It!"
end

function FPSBooster.Disable()
    if not FPSBooster.Enabled then return false, "Already disabled" end
    
    FPSBooster.Enabled = false
    
    -- Restore objects
    for _, obj in ipairs(workspace:GetDescendants()) do
        restoreObject(obj)
    end
    
    -- Restore Terrain
    if Terrain and originalStates.waterProperties then
        pcall(function()
            Terrain.WaterReflectance = originalStates.waterProperties.WaterReflectance
            Terrain.WaterWaveSize = originalStates.waterProperties.WaterWaveSize
            Terrain.WaterWaveSpeed = originalStates.waterProperties.WaterWaveSpeed
        end)
    end
    
    -- Restore Lighting
    if originalStates.lighting.GlobalShadows ~= nil then
        Lighting.GlobalShadows = originalStates.lighting.GlobalShadows
        Lighting.FogEnd = originalStates.lighting.FogEnd
        Lighting.FogStart = originalStates.lighting.FogStart
    end
    
    -- Restore Post Effects
    for effect, state in pairs(originalStates.effects) do
        if effect and effect.Parent then effect.Enabled = state end
    end
    
    -- Restore Quality
    settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
    
    -- Disconnect
    if newObjectConnection then
        newObjectConnection:Disconnect()
        newObjectConnection = nil
    end
    
    originalStates = { reflectance = {}, transparency = {}, lighting = {}, effects = {}, waterProperties = {} }
    
    return true, "FPS Booster disabled - visuals restored"
end

FPSBooster.IsEnabled = function() return FPSBooster.Enabled end

-- ===================== GUI TOGGLE =====================

local ToggleFPS = Tab5:Toggle({
    Title = "Enable FPS Booster",
    Desc = "Boost FPS by disabling shadows, fog, particles, water waves, etc. (great for Fish It lag)",
    Icon = "gauge",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            local success, msg = FPSBooster.Enable()
            if success then
                WindUI:Notify({
                    Title = "FPS Booster Activated",
                    Content = msg .. "\nGame looks simpler but way smoother!",
                    Duration = 5,
                    Icon = "gauge"
                })
            else
                WindUI:Notify({Title = "FPS Booster", Content = msg, Duration = 4})
            end
        else
            local success, msg = FPSBooster.Disable()
            WindUI:Notify({
                Title = "FPS Booster Deactivated",
                Content = msg .. "\nVisuals kembali normal",
                Duration = 5,
                Icon = "gauge-off"
            })
        end
    end
})
-- =================================================
--     🌊 ULTRA STABLE WALK ON WATER V3.2 🌊
--     Raycast-only, anti-stuck, invisible platform
--     Paste SETELAH FPS Booster di Tab5 (Settings)
-- =================================================

local SectionWalkWater = Tab5:Section({
    Title = "Walk On Water (Ultra Stable)",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE WALK ON WATER =====================
local WalkOnWater = {
    Enabled = false,
    Platform = nil,
    AlignPos = nil,
    Connection = nil
}

local PLATFORM_SIZE = 14
local OFFSET = 3
local LAST_WATER_Y = nil

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

local function GetCharacterReferences()
    local char = LocalPlayer.Character
    if not char then return end

    local humanoid = char:FindFirstChildOfClass("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not humanoid or not hrp then return end

    return char, humanoid, hrp
end

local function ForceSurfaceLift()
    local _, humanoid, hrp = GetCharacterReferences()
    if not humanoid or not hrp then return end

    if humanoid:GetState() ~= Enum.HumanoidStateType.Swimming then return end

    for _ = 1, 60 do
        hrp.Velocity = Vector3.new(0, 80, 0)
        task.wait(0.03)

        if humanoid:GetState() ~= Enum.HumanoidStateType.Swimming then break end
    end

    hrp.CFrame = hrp.CFrame + Vector3.new(0, 3, 0)
end

local function GetWaterHeight()
    local _, _, hrp = GetCharacterReferences()
    if not hrp then return LAST_WATER_Y end

    local origin = hrp.Position + Vector3.new(0, 5, 0)

    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Blacklist
    params.FilterDescendantsInstances = { LocalPlayer.Character }
    params.IgnoreWater = false

    local result = Workspace:Raycast(origin, Vector3.new(0, -600, 0), params)

    if result then
        LAST_WATER_Y = result.Position.Y
        return LAST_WATER_Y
    end

    return LAST_WATER_Y
end

local function CreatePlatform()
    if WalkOnWater.Platform then WalkOnWater.Platform:Destroy() end

    local p = Instance.new("Part")
    p.Size = Vector3.new(PLATFORM_SIZE, 1, PLATFORM_SIZE)
    p.Anchored = true
    p.CanCollide = true
    p.Transparency = 1
    p.CanQuery = false
    p.CanTouch = false
    p.Name = "WaterLockPlatform"
    p.Parent = Workspace

    WalkOnWater.Platform = p
end

local function SetupAlign()
    local _, _, hrp = GetCharacterReferences()
    if not hrp then return false end

    if WalkOnWater.AlignPos then WalkOnWater.AlignPos:Destroy() end

    local att = hrp:FindFirstChild("RootAttachment")
    if not att then
        att = Instance.new("Attachment")
        att.Name = "RootAttachment"
        att.Parent = hrp
    end

    local ap = Instance.new("AlignPosition")
    ap.Attachment0 = att
    ap.MaxForce = math.huge
    ap.MaxVelocity = math.huge
    ap.Responsiveness = 200
    ap.RigidityEnabled = true
    ap.Parent = hrp

    WalkOnWater.AlignPos = ap
    return true
end

local function Cleanup()
    if WalkOnWater.Connection then
        WalkOnWater.Connection:Disconnect()
        WalkOnWater.Connection = nil
    end

    if WalkOnWater.AlignPos then
        WalkOnWater.AlignPos:Destroy()
        WalkOnWater.AlignPos = nil
    end

    if WalkOnWater.Platform then
        WalkOnWater.Platform:Destroy()
        WalkOnWater.Platform = nil
    end
end

function WalkOnWater.Start()
    if WalkOnWater.Enabled then return end

    local char, humanoid, hrp = GetCharacterReferences()
    if not char or not humanoid or not hrp then return end

    ForceSurfaceLift()

    WalkOnWater.Enabled = true
    LAST_WATER_Y = nil

    CreatePlatform()
    if not SetupAlign() then
        WalkOnWater.Enabled = false
        Cleanup()
        return
    end

    WalkOnWater.Connection = RunService.Heartbeat:Connect(function()
        if not WalkOnWater.Enabled then return end

        local _, _, currentHRP = GetCharacterReferences()
        if not currentHRP then return end

        local waterY = GetWaterHeight()
        if not waterY then return end

        if WalkOnWater.Platform then
            WalkOnWater.Platform.CFrame = CFrame.new(
                currentHRP.Position.X,
                waterY - 0.5,
                currentHRP.Position.Z
            )
        end

        if WalkOnWater.AlignPos then
            WalkOnWater.AlignPos.Position = Vector3.new(
                currentHRP.Position.X,
                waterY + OFFSET,
                currentHRP.Position.Z
            )
        end
    end)
end

function WalkOnWater.Stop()
    WalkOnWater.Enabled = false
    Cleanup()
end

-- Respawn handler
LocalPlayer.CharacterAdded:Connect(function()
    if WalkOnWater.Enabled then
        task.wait(0.5)
        Cleanup()
        WalkOnWater.Enabled = false
        WalkOnWater.Start()
    end
end)

local Colorpicker = Tab5:Colorpicker({
    Title = "Colorpicker",
    Desc = "Background Colorpicker (need update)",
    Default = Color3.fromRGB(0, 255, 0),
    Transparency = 0,
    Locked = false,
    Callback = function(color) 
        print("Background color: " .. tostring(color))
    end
})

local Tab6 = Window:Tab({
    Title = "Premium",
    Icon = "star",
})

-- =================================================
--     BLATANT V4 - HYBRID ENHANCED (STABIL & GACOR)
--     Multiple fires + pre-cancel + parallel + delay mikro
--     Clean, no aneh-aneh nama, GUI WindUI
--     Paste di tempat Ultra Blatant lu (ganti yang lama)
-- =================================================

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")

local netFolder = ReplicatedStorage
    :WaitForChild("Packages")
    :WaitForChild("_Index")
    :WaitForChild("sleitnick_net@0.2.0")
    :WaitForChild("net")

local RF_ChargeFishingRod = netFolder:WaitForChild("RF/ChargeFishingRod")
local RF_RequestMinigame = netFolder:WaitForChild("RF/RequestFishingMinigameStarted")
local RF_CancelFishingInputs = netFolder:WaitForChild("RF/CancelFishingInputs")
local RF_UpdateAutoFishingState = netFolder:WaitForChild("RF/UpdateAutoFishingState")
local RE_FishingCompleted = netFolder:WaitForChild("RE/FishingCompleted")
local RE_MinigameChanged = netFolder:WaitForChild("RE/FishingMinigameChanged")

-- Module table
local BlatantV4 = {}
BlatantV4.Active = false
BlatantV4.Stats = {
    castCount = 0,
    startTime = 0
}

BlatantV4.Settings = {
    CompleteDelay = 0.673,    -- delay sebelum complete (default V4)
    CancelDelay = 0.279,      -- delay sebelum cancel
    CompleteCount = 3,        -- jumlah spam complete per cycle (bisa diubah)
    MicroDelay = 0.005        -- delay kecil antar charge/request (anti jeda)
}

local function safeFire(func)
    task.spawn(function()
        pcall(func)
    end)
end

local function blatantV4Loop()
    while BlatantV4.Active do
        local currentTime = Workspace:GetServerTimeNow() or tick()
        
        -- Pre-Cancel (biar server reset state)
        safeFire(function()
            RF_CancelFishingInputs:InvokeServer()
        end)
        
        -- Charge & Request (parallel + delay mikro)
        safeFire(function()
            RF_ChargeFishingRod:InvokeServer({[1] = currentTime})
        end)
        
        task.wait(BlatantV4.Settings.MicroDelay)  -- anti jeda, seperti KaiiZo
        
        safeFire(function()
            RF_RequestMinigame:InvokeServer(1, 0.999, currentTime)
        end)
        
        BlatantV4.Stats.castCount = BlatantV4.Stats.castCount + 1
        
        task.wait(BlatantV4.Settings.CompleteDelay)
        
        -- Spam complete (default 3x, bisa diubah)
        for i = 1, BlatantV4.Settings.CompleteCount do
            safeFire(function()
                RE_FishingCompleted:FireServer()
            end)
        end
        
        task.wait(BlatantV4.Settings.CancelDelay)
        safeFire(function()
            RF_CancelFishingInputs:InvokeServer()
        end)
    end
end

-- Backup listener (event MinigameChanged)
RE_MinigameChanged.OnClientEvent:Connect(function(state)
    if not BlatantV4.Active then return end
    
    task.spawn(function()
        task.wait(BlatantV4.Settings.CompleteDelay)
        
        for i = 1, BlatantV4.Settings.CompleteCount do
            safeFire(function()
                RE_FishingCompleted:FireServer()
            end)
        end
        
        task.wait(BlatantV4.Settings.CancelDelay)
        safeFire(function()
            RF_CancelFishingInputs:InvokeServer()
        end)
    end)
end)

-- Update Settings
function BlatantV4.UpdateSettings(completeDelay, cancelDelay, completeCount)
    if completeDelay ~= nil then
        BlatantV4.Settings.CompleteDelay = completeDelay
    end
    if cancelDelay ~= nil then
        BlatantV4.Settings.CancelDelay = cancelDelay
    end
    if completeCount ~= nil then
        BlatantV4.Settings.CompleteCount = completeCount
    end
end

-- Start
function BlatantV4.Start()
    if BlatantV4.Active then 
        print("⚠️ Blatant V4 sudah nyala!")
        return
    end
    
    BlatantV4.Active = true
    BlatantV4.Stats.castCount = 0
    BlatantV4.Stats.startTime = tick()
    
    task.spawn(blatantV4Loop)
    print("🚀 Blatant V4 started! (gacor stabil - multiple complete)")
end

-- Stop (matikan auto bawaan, cancel inputs)
function BlatantV4.Stop()
    if not BlatantV4.Active then return end
    
    BlatantV4.Active = false
    
    safeFire(function()
        RF_UpdateAutoFishingState:InvokeServer(false)  -- MATIKAN auto bawaan
    end)
    
    task.wait(0.3)  -- delay lebih panjang biar auto mati beneran
    
    safeFire(function()
        RF_CancelFishingInputs:InvokeServer()
    end)
    
    safeFire(function()
        RF_CancelFishingInputs:InvokeServer()  -- 2x cancel biar stop total
    end)
    
    print("🛑 Blatant V4 stopped - Auto fishing game dimatikan")
end

-- ===================== GUI BLATANT V4 =====================

local SectionV4 = Tab6:Section({
    Title = "Blatant Stable",
    TextXAlignment = "Left",
    TextSize = 17,
})

local ToggleV4 = Tab6:Toggle({
    Title = "Ultra Blatant V4",
    Desc = "Gacor stabil nonstop (multiple complete + delay mikro)",
    Icon = "zap",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            BlatantV4.Start()
            WindUI:Notify({
                Title = "Blatant V4 Activated",
                Content = "Gacor nonstop! Complete x" .. BlatantV4.Settings.CompleteCount .. " | Delay: " .. BlatantV4.Settings.CompleteDelay .. "s",
                Duration = 5,
                Icon = "zap"
            })
        else
            BlatantV4.Stop()
            WindUI:Notify({
                Title = "Blatant V4 Stopped",
                Content = "Stopped - Auto fishing game dimatikan",
                Duration = 5,
                Icon = "shield-off"
            })
        end
    end
})

local SectionV4Settings = Tab6:Section({
    Title = "V4 Settings (Stabil)",
    TextXAlignment = "Left",
    TextSize = 16,
})

local InputComplete = Tab6:Input({
    Title = "Complete Delay",
    Desc = "Waktu sebelum complete (default 0.673, naikkan kalau jeda)",
    Value = tostring(BlatantV4.Settings.CompleteDelay),
    Placeholder = "0.673",
    InputIcon = "timer",
    Type = "Input",
    Callback = function(val)
        local num = tonumber(val)
        if num and num >= 0 then
            BlatantV4.UpdateSettings(num, nil, nil)
            WindUI:Notify({
                Title = "Delay Updated",
                Content = "Complete Delay: " .. num .. "s",
                Duration = 3
            })
        end
    end
})

local InputCancel = Tab6:Input({
    Title = "Cancel Delay",
    Desc = "Waktu sebelum cancel (default 0.279)",
    Value = tostring(BlatantV4.Settings.CancelDelay),
    Placeholder = "0.279",
    InputIcon = "timer-reset",
    Type = "Input",
    Callback = function(val)
        local num = tonumber(val)
        if num and num >= 0 then
            BlatantV4.UpdateSettings(nil, num, nil)
            WindUI:Notify({
                Title = "Delay Updated",
                Content = "Cancel Delay: " .. num .. "s",
                Duration = 3
            })
        end
    end
})

local InputCompleteCount = Tab6:Input({
    Title = "Complete Spam Count",
    Desc = "Jumlah spam complete per cycle (3-10 recommended)",
    Value = tostring(BlatantV4.Settings.CompleteCount),
    Placeholder = "3",
    InputIcon = "zap",
    Type = "Input",
    Callback = function(val)
        local num = tonumber(val)
        if num and num >= 1 then
            BlatantV4.UpdateSettings(nil, nil, num)
            WindUI:Notify({
                Title = "Spam Count Updated",
                Content = "Complete spam x" .. num .. " per cycle",
                Duration = 3
            })
        end
    end
})

-- Stats console
task.spawn(function()
    while true do
        task.wait(5)
        if BlatantV4.Active then
            local uptime = math.floor(tick() - BlatantV4.Stats.startTime)
            print(string.format("[BlatantV4] Casts: %d | Uptime: %d detik | Delay: %.3f / %.3f | Spam: %d",
                BlatantV4.Stats.castCount, uptime,
                BlatantV4.Settings.CompleteDelay, BlatantV4.Settings.CancelDelay,
                BlatantV4.Settings.CompleteCount))
        end
    end
end)


-- =================================================
--     ULTRA TESTER - SINGLE MODE (PERSIS SAMA LOGIC)
--     Single complete per cycle, delay adjustable
--     Paste SETELAH return UltraBlatant end)()
-- =================================================

-- ===================== MODULE ULTRA TESTER =====================
local UltraTester = {}
UltraTester.Active = false
UltraTester.Stats = {
    castCount = 0,
    startTime = 0
}

UltraTester.Settings = {
    CompleteDelay = 0.001,
    CancelDelay = 0.001
}

local function safeFire(func)
    task.spawn(function()
        pcall(func)
    end)
end

local function ultraTesterSpamLoop()
    while UltraTester.Active do
        local currentTime = tick()
        
        safeFire(function()
            RF_ChargeFishingRod:InvokeServer({[1] = currentTime})
        end)
        task.wait(0.005)
        safeFire(function()
            RF_RequestMinigame:InvokeServer(1, 0, currentTime)
        end)
        
        UltraTester.Stats.castCount = UltraTester.Stats.castCount + 1
        
        task.wait(UltraTester.Settings.CompleteDelay)
        
        safeFire(function()
            RE_FishingCompleted:FireServer()
        end)
        
        task.wait(UltraTester.Settings.CancelDelay)
        safeFire(function()
            RF_CancelFishingInputs:InvokeServer()
        end)
    end
end

RE_MinigameChanged.OnClientEvent:Connect(function(state)
    if not UltraTester.Active then return end
    
    task.spawn(function()
        task.wait(UltraTester.Settings.CompleteDelay)
        
        safeFire(function()
            RE_FishingCompleted:FireServer()
        end)
        
        task.wait(UltraTester.Settings.CancelDelay)
        safeFire(function()
            RF_CancelFishingInputs:InvokeServer()
        end)
    end)
end)

function UltraTester.UpdateSettings(completeDelay, cancelDelay)
    if completeDelay ~= nil then
        UltraTester.Settings.CompleteDelay = completeDelay
        print("✅ UltraTester CompleteDelay updated:", completeDelay)
    end
    
    if cancelDelay ~= nil then
        UltraTester.Settings.CancelDelay = cancelDelay
        print("✅ UltraTester CancelDelay updated:", cancelDelay)
    end
end

function UltraTester.Start()
    if UltraTester.Active then 
        print("⚠️ Ultra Tester sudah nyala!")
        return
    end
    
    UltraTester.Active = true
    UltraTester.Stats.castCount = 0
    UltraTester.Stats.startTime = tick()
    
    task.spawn(ultraTesterSpamLoop)
end

function UltraTester.Stop()
    if not UltraTester.Active then return end
    
    UltraTester.Active = false
    
    safeFire(function()
        RF_UpdateAutoFishingState:InvokeServer(true)
    end)
    
    task.wait(0.2)
    
    safeFire(function()
        RF_CancelFishingInputs:InvokeServer()
    end)
    
    print("✅ Ultra Tester stopped")
end

-- ===================== GUI ULTRA TESTER =====================

local SectionTester = Tab6:Section({
    Title = "Ultra Blatant",
    TextXAlignment = "Left",
    TextSize = 17,
})

local ToggleTester = Tab6:Toggle({
    Title = "Ultra Tester Auto Fish",
    Desc = "Single mode blatant fishing (gacor stabil)",
    Icon = "fish",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        _G.UltraTesterEnabled = state
        
        if state then
            UltraTester.Start()
            WindUI:Notify({
                Title = "Ultra Tester Activated",
                Content = "Single mode ON! Delay: " .. UltraTester.Settings.CompleteDelay .. " / " .. UltraTester.Settings.CancelDelay,
                Duration = 5,
                Icon = "fish"
            })
        else
            UltraTester.Stop()
            WindUI:Notify({
                Title = "Ultra Tester Stopped",
                Content = "Stopped - Game auto fishing re-enabled",
                Duration = 5,
                Icon = "shield-off"
            })
        end
    end
})

local SectionTesterSettings = Tab6:Section({
    Title = "Tester Settings (Adjust Delays)",
    TextXAlignment = "Left",
    TextSize = 16,
})

local InputCompleteTester = Tab6:Input({
    Title = "Complete Delay",
    Desc = "Waktu tunggu sebelum complete (default 0.001)",
    Value = tostring(UltraTester.Settings.CompleteDelay),
    Placeholder = "0.001 - 0.5",
    InputIcon = "timer",
    Type = "Input",
    Callback = function(input)
        local val = tonumber(input)
        if val and val >= 0 then
            UltraTester.UpdateSettings(val, nil)
            WindUI:Notify({
                Title = "Delay Updated",
                Content = "Complete Delay: " .. val,
                Duration = 3
            })
        else
            warn("⚠️ Complete Delay harus angka >= 0!")
        end
    end
})

local InputCancelTester = Tab6:Input({
    Title = "Cancel Delay",
    Desc = "Waktu tunggu setelah complete sebelum cancel",
    Value = tostring(UltraTester.Settings.CancelDelay),
    Placeholder = "0.001 - 0.5",
    InputIcon = "timer-reset",
    Type = "Input",
    Callback = function(input)
        local val = tonumber(input)
        if val and val >= 0 then
            UltraTester.UpdateSettings(nil, val)
            WindUI:Notify({
                Title = "Delay Updated",
                Content = "Cancel Delay: " .. val,
                Duration = 3
            })
        else
            warn("⚠️ Cancel Delay harus angka >= 0!")
        end
    end
})

-- Stats console
task.spawn(function()
    while true do
        task.wait(5)
        if UltraTester.Active then
            local uptime = math.floor(tick() - UltraTester.Stats.startTime)
            print(string.format("[UltraTester] Casts: %d | Uptime: %d detik | Delays: %.3f / %.3f",
                UltraTester.Stats.castCount, uptime,
                UltraTester.Settings.CompleteDelay, UltraTester.Settings.CancelDelay))
        end
    end
end)

local ScanButton = Tab6:Button({
    Title = "Scan Fish Remotes",
    Desc = "Search for remote with the word 'fish'",
    Callback = function()
        scanRemotes()
    end
})
-- =================================================
--     ⚡ SKIN ANIMATION REPLACER (Fish Caught Only)

local SectionSkinReplacer = Tab6:Section({
    Title = "Skin Animation Replacer (Fish Caught)",
    TextXAlignment = "Left",
    TextSize = 17,
})

-- ===================== MODULE SKIN ANIMATION REPLACER =====================
local SkinAnimationReplacer = {}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local humanoid = char:WaitForChild("Humanoid")

local Animator = humanoid:FindFirstChildOfClass("Animator")
if not Animator then
    Animator = Instance.new("Animator", humanoid)
end

local SkinDatabase = {
    ["Eclipse"] = "rbxassetid://107940819382815",
    ["HolyTrident"] = "rbxassetid://128167068291703",
    ["SoulScythe"] = "rbxassetid://82259219343456",
    ["OceanicHarpoon"] = "rbxassetid://76325124055693",
    ["BinaryEdge"] = "rbxassetid://109653945741202",
    ["Vanquisher"] = "rbxassetid://93884986836266",
    ["KrampusScythe"] = "rbxassetid://134934781977605",
    ["BanHammer"] = "rbxassetid://96285280763544",
    ["CorruptionEdge"] = "rbxassetid://126613975718573",
    ["PrincessParasol"] = "rbxassetid://99143072029495"
}

local CurrentSkin = nil
local AnimationPool = {}
local IsEnabled = false
local POOL_SIZE = 3

local killedTracks = {}
local replaceCount = 0
local currentPoolIndex = 1

local function LoadAnimationPool(skinId)
    local animId = SkinDatabase[skinId]
    if not animId then return false end
    
    -- Clear old pool
    for _, track in ipairs(AnimationPool) do
        pcall(function() track:Stop(0) track:Destroy() end)
    end
    AnimationPool = {}
    
    local anim = Instance.new("Animation")
    anim.AnimationId = animId
    anim.Name = "CUSTOM_SKIN_ANIM"
    
    for i = 1, POOL_SIZE do
        local track = Animator:LoadAnimation(anim)
        track.Priority = Enum.AnimationPriority.Action4
        track.Looped = false
        track.Name = "SKIN_POOL_" .. i
        
        -- Pre-cache
        task.spawn(function()
            pcall(function()
                track:Play(0, 1, 0)
                task.wait(0.05)
                track:Stop(0)
            end)
        end)
        
        table.insert(AnimationPool, track)
    end
    
    currentPoolIndex = 1
    return true
end

local function GetNextTrack()
    for i = 1, POOL_SIZE do
        local track = AnimationPool[i]
        if track and not track.IsPlaying then return track end
    end
    
    currentPoolIndex = currentPoolIndex % POOL_SIZE + 1
    return AnimationPool[currentPoolIndex]
end

local function IsFishCaughtAnimation(track)
    if not track or not track.Animation then return false end
    
    local trackName = string.lower(track.Name or "")
    local animName = string.lower(track.Animation.Name or "")
    
    return string.find(trackName, "fishcaught") or string.find(trackName, "caught") or
           string.find(animName, "fishcaught") or string.find(animName, "caught")
end

local function InstantReplace(originalTrack)
    local nextTrack = GetNextTrack()
    if not nextTrack then return end
    
    replaceCount = replaceCount + 1
    killedTracks[originalTrack] = tick()
    
    task.spawn(function()
        for i = 1, 10 do
            pcall(function()
                if originalTrack.IsPlaying then
                    originalTrack:Stop(0)
                    originalTrack:AdjustSpeed(0)
                    originalTrack.TimePosition = 0
                end
            end)
            task.wait()
        end
    end)
    
    pcall(function()
        if nextTrack.IsPlaying then nextTrack:Stop(0) end
        nextTrack:Play(0, 1, 1)
        nextTrack:AdjustSpeed(1)
    end)
    
    task.delay(1, function() killedTracks[originalTrack] = nil end)
end

-- Hooks & Monitors (aggressive tapi optimized)
humanoid.AnimationPlayed:Connect(function(track)
    if not IsEnabled then return end
    if IsFishCaughtAnimation(track) then
        task.spawn(function() InstantReplace(track) end)
    end
end)

RunService.RenderStepped:Connect(function()
    if not IsEnabled then return end
    local tracks = humanoid:GetPlayingAnimationTracks()
    for _, track in ipairs(tracks) do
        if string.find(string.lower(track.Name or ""), "skin_pool") then continue end
        
        if killedTracks[track] and track.IsPlaying then
            pcall(function() track:Stop(0) track:AdjustSpeed(0) end)
        elseif track.IsPlaying and IsFishCaughtAnimation(track) then
            task.spawn(function() InstantReplace(track) end)
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if not IsEnabled then return end
    local tracks = humanoid:GetPlayingAnimationTracks()
    for _, track in ipairs(tracks) do
        if string.find(string.lower(track.Name or ""), "skin_pool") then continue end
        if killedTracks[track] and track.IsPlaying then
            pcall(function() track:Stop(0) track:AdjustSpeed(0) end)
        end
    end
end)

RunService.Stepped:Connect(function()
    if not IsEnabled then return end
    for track, _ in pairs(killedTracks) do
        if track and track.IsPlaying then
            pcall(function() track:Stop(0) track:AdjustSpeed(0) end)
        end
    end
end)

-- Respawn handler
player.CharacterAdded:Connect(function(newChar)
    task.wait(1.5)
    char = newChar
    humanoid = char:WaitForChild("Humanoid")
    Animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
    
    killedTracks = {}
    replaceCount = 0
    
    if IsEnabled and CurrentSkin then
        task.wait(0.5)
        LoadAnimationPool(CurrentSkin)
    end
end)

-- Public API
SkinAnimationReplacer.SwitchSkin = function(skinId)
    if not SkinDatabase[skinId] then return false end
    CurrentSkin = skinId
    if IsEnabled then return LoadAnimationPool(skinId) end
    return true
end

SkinAnimationReplacer.Enable = function()
    if IsEnabled or not CurrentSkin then return false end
    local success = LoadAnimationPool(CurrentSkin)
    if success then
        IsEnabled = true
        killedTracks = {}
        replaceCount = 0
        return true
    end
    return false
end

SkinAnimationReplacer.Disable = function()
    if not IsEnabled then return false end
    IsEnabled = false
    killedTracks = {}
    replaceCount = 0
    for _, track in ipairs(AnimationPool) do pcall(function() track:Stop(0) end) end
    return true
end

SkinAnimationReplacer.IsEnabled = function() return IsEnabled end
SkinAnimationReplacer.GetCurrentSkin = function() return CurrentSkin end
SkinAnimationReplacer.GetReplaceCount = function() return replaceCount end

-- ===================== GUI CONTROLS =====================

local ToggleReplacer = Tab6:Toggle({
    Title = "Enable Fish Caught Animation Replacer",
    Desc = "Ganti animasi pas ikan kena jadi custom skin (super smooth)",
    Icon = "sparkles",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        if state then
            if SkinAnimationReplacer.Enable() then
                WindUI:Notify({
                    Title = "Animation Replacer",
                    Content = "Activated! Skin: " .. (CurrentSkin or "None") .. "\nReplaces: " .. replaceCount,
                    Duration = 5,
                    Icon = "sparkles"
                })
            else
                WindUI:Notify({Title = "Error", Content = "Pilih skin dulu!", Duration = 4})
            end
        else
            SkinAnimationReplacer.Disable()
            WindUI:Notify({
                Title = "Animation Replacer",
                Content = "Disabled - Animasi default kembali",
                Duration = 5,
                Icon = "sparkles-off"
            })
        end
    end
})

local DropdownSkinReplacer = Tab6:Dropdown({
    Title = "Select Skin for Fish Caught",
    Values = {"Eclipse", "HolyTrident", "SoulScythe", "OceanicHarpoon", "BinaryEdge", "Vanquisher", "KrampusScythe", "BanHammer", "CorruptionEdge", "PrincessParasol"},
    Default = "Eclipse",
    Callback = function(Value)
        if SkinAnimationReplacer.SwitchSkin(Value) then
            WindUI:Notify({
                Title = "Skin Changed",
                Content = "Fish Caught Animation: " .. Value .. "\n(Enable replacer untuk apply)",
                Duration = 4,
                Icon = "refresh-cw"
            })
        else
            warn("Skin ID tidak ditemukan: " .. Value)
        end
    end
})

-- Optional: Tampilkan stats replace count di console setiap 10 detik
task.spawn(function()
    while true do
        task.wait(10)
        if IsEnabled then
            print(string.format("[Skin Replacer] Active | Skin: %s | Replaces: %d", CurrentSkin or "None", replaceCount))
        end
    end
end)

local Tab7 = Window:Tab({
    Title = "Automation",
    Icon = "loader",
})

-- =================================================
--     AUTO FAVORITE - MINIMAL & STABIL
--     Toggle + Dropdown multi, instant nyala
--     No delay pertama toggle, anti nil error
-- =================================================

local AutoFavorite = {}

local AUTO_FAVORITE_TIERS = {}
local AUTO_FAVORITE_VARIANTS = {}
local AUTO_FAVORITE_ENABLED = false

local activeConnection = nil
local fishDataCache = {}

-- Ambil remote dengan aman (retry kalau nil)
local FavoriteEvent, NotificationEvent
local function getRemotes()
    if FavoriteEvent and NotificationEvent then return true end
    
    pcall(function()
        local packages = ReplicatedStorage:WaitForChild("Packages", 5)
        local index = packages:WaitForChild("_Index", 5)
        local sleitnick = index:WaitForChild("sleitnick_net@0.2.0", 5)
        local net = sleitnick:WaitForChild("net", 5)
        
        FavoriteEvent = net:WaitForChild("RE/FavoriteItem", 5)
        NotificationEvent = net:WaitForChild("RE/ObtainedNewFishNotification", 5)
    end)
    
    return FavoriteEvent and NotificationEvent
end

local itemsModule = require(ReplicatedStorage:WaitForChild("Items", 5))

local function getFishData(itemId)
    if fishDataCache[itemId] then return fishDataCache[itemId] end
    
    for _, fish in pairs(itemsModule) do
        if fish.Data and fish.Data.Id == itemId then
            fishDataCache[itemId] = fish
            return fish
        end
    end
    return nil
end

local function disconnectConnection()
    if activeConnection then
        pcall(function() activeConnection:Disconnect() end)
        activeConnection = nil
    end
end

local function startMonitoring()
    disconnectConnection()
    
    if not getRemotes() then
        WindUI:Notify({Title = "Auto Favorite", Content = "Gagal load remote, coba restart script", Duration = 5, Icon = "alert-triangle"})
        return
    end
    
    activeConnection = NotificationEvent.OnClientEvent:Connect(function(itemId, metadata, extraData)
        if not AUTO_FAVORITE_ENABLED then return end
        
        local inventoryItem = extraData and extraData.InventoryItem
        local uuid = inventoryItem and inventoryItem.UUID
        
        if not uuid or inventoryItem.Favorited then return end

        local shouldFavorite = false

        local fishData = getFishData(itemId)
        if fishData and fishData.Data and fishData.Data.Tier then
            if AUTO_FAVORITE_TIERS[fishData.Data.Tier] then shouldFavorite = true end
        end

        if not shouldFavorite then
            local variantId = metadata and metadata.VariantId
            if variantId and variantId ~= "None" and AUTO_FAVORITE_VARIANTS[variantId] then shouldFavorite = true end
        end

        if shouldFavorite then
            task.delay(0.35, function()
                pcall(function()
                    FavoriteEvent:FireServer(uuid)
                end)
            end)
        end
    end)
end

-- Toggle
local ToggleAutoFav = Tab7:Toggle({
    Title = "Aktifkan Auto Favorite",
    Desc = "Otomatis favorite ikan baru sesuai filter tier/variant",
    Icon = "star",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        AUTO_FAVORITE_ENABLED = state
        if state then
            startMonitoring()
            WindUI:Notify({
                Title = "Auto Favorite",
                Content = "Aktif - Memantau ikan baru",
                Duration = 4,
                Icon = "star"
            })
        else
            disconnectConnection()
            WindUI:Notify({
                Title = "Auto Favorite",
                Content = "Nonaktif - Monitoring dimatikan",
                Duration = 4,
                Icon = "star-off"
            })
        end
    end
})

-- Dropdown Tier
local DropdownTiers = Tab7:Dropdown({
    Title = "Pilih Tier",
    Desc = "Tier yang akan di-auto favorite (multi)",
    Values = {"Common", "Uncommon", "Rare", "Epic", "Legendary", "Mythic", "SECRET"},
    Multi = true,
    AllowNone = true,
    Callback = function(selected)
        table.clear(AUTO_FAVORITE_TIERS)
        for _, tierName in ipairs(selected) do
            local tier = TIER_MAP[tierName]
            if tier then AUTO_FAVORITE_TIERS[tier] = true end
        end
        WindUI:Notify({
            Title = "Tier Diperbarui",
            Content = #selected > 0 and "Tier aktif: " .. table.concat(selected, ", ") or "Tidak ada tier dipilih",
            Duration = 4,
            Icon = "star"
        })
    end
})

-- Dropdown Variant
local DropdownVariants = Tab7:Dropdown({
    Title = "Pilih Variant",
    Desc = "Variant yang akan di-auto favorite (multi)",
    Values = {
        "Galaxy", "Corrupt", "Gemstone", "Fairy Dust", "Midnight", "Color Burn",
        "Holographic", "Lightning", "Radioactive", "Ghost", "Gold", "Frozen",
        "1x1x1x1", "Stone", "Sandy", "Noob", "Moon Fragment", "Festive",
        "Albino", "Arctic Frost", "Disco"
    },
    Multi = true,
    AllowNone = true,
    Callback = function(selected)
        table.clear(AUTO_FAVORITE_VARIANTS)
        for _, var in ipairs(selected) do
            AUTO_FAVORITE_VARIANTS[var] = true
        end
        WindUI:Notify({
            Title = "Variant Diperbarui",
            Content = #selected > 0 and "Variant aktif: " .. table.concat(selected, ", ") or "Tidak ada variant dipilih",
            Duration = 4,
            Icon = "star"
        })
    end
})

local SectionNoAnim = Tab7:Section({
    Title = "No Animation",
    TextXAlignment = "Left",
    TextSize = 17,
})

local isNoAnimationActive = false
local originalAnimator = nil
local originalAnimateScript = nil

local function DisableAnimations()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    
    if not humanoid then return end

    local animateScript = character:FindFirstChild("Animate")
    if animateScript and animateScript:IsA("LocalScript") and animateScript.Enabled then
        originalAnimateScript = animateScript.Enabled
        animateScript.Enabled = false
    end

    local animator = humanoid:FindFirstChildOfClass("Animator")
    if animator then
        originalAnimator = animator
        animator:Destroy()
    end
end

local function EnableAnimations()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    
    local animateScript = character:FindFirstChild("Animate")
    if animateScript and originalAnimateScript ~= nil then
        animateScript.Enabled = originalAnimateScript
    end
    
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end

    local existingAnimator = humanoid:FindFirstChildOfClass("Animator")
    if not existingAnimator then
        if originalAnimator and not originalAnimator.Parent then
            originalAnimator.Parent = humanoid
        else
            Instance.new("Animator").Parent = humanoid
        end
    end
    originalAnimator = nil
end

-- Handler respawn
LocalPlayer.CharacterAdded:Connect(function(newCharacter)
    if isNoAnimationActive then
        task.wait(0.2)
        DisableAnimations()
    end
end)

local ToggleNoAnim = Tab7:Toggle({
    Title = "Aktifkan No Animation",
    Desc = "Nonaktifkan semua animasi karakter (tetap bisa gerak normal)",
    Icon = "pause",
    Type = "Checkbox",
    Default = false,
    Callback = function(state)
        isNoAnimationActive = state
        if state then
            DisableAnimations()
            WindUI:Notify({
                Title = "No Animation",
                Content = "Aktif - Semua animasi dimatikan",
                Duration = 3,
                Icon = "zap"
            })
        else
            EnableAnimations()
            WindUI:Notify({
                Title = "No Animation",
                Content = "Nonaktif - Animasi kembali normal",
                Duration = 3,
                Icon = "x"
            })
        end
    end
})